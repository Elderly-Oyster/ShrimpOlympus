namespace Services.User
{
    public interface IUserService
    {
        // string GetUserId();
        // bool IsAnonymous { get; }
        // bool IsFirstLaunch { get; }
        // Task<User> GetUser(bool force = false);
        // event Action<User> OnUserUpdated;
        // Task<bool> LoginInWithFacebook(bool link);
        // Task<bool> LoginInWithApple(bool link);
        // Task<bool> LoginInWithGooglePlayGames(bool link);
        // Task ModifyUser(RevenueDataDto revenueDataDto, UserDataDto userDataDto);
        // Task<(UserData, UserStats)> GetUserData(string userId);
    }
}